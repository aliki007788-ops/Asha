version: "3.8"

services:
  asha-api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - ASHA_ENV=production
      - ASHA_LOG_LEVEL=INFO
    restart: unless-stopped
